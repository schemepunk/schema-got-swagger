const sgsConfig = require('./sgsConfig');
const semverish = require('./semverish');
const semveristConfig = require('./semveristConfig');
const schemePunkConfig = require('./schemePunkConfig');
const semverishMainData = require('./semverishMainData');
const swaggerMainTemplates = require('./swaggerMainTemplates');

module.exports = {
  schemePunkConfig,
  sgsConfig,
  semverish,
  semveristConfig,
  semverishMainData,
  swaggerMainTemplates,
};
